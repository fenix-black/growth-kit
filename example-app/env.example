# GrowthKit Configuration (Secure Proxy Mode)
# Get your API key from the GrowthKit database or admin dashboard
# These variables are server-side only and never exposed to the client
GROWTHKIT_API_KEY=your-api-key-here
GROWTHKIT_API_URL=https://growth.fenixblack.ai/api

# Note: NEXT_PUBLIC_GROWTHKIT_* variables are no longer needed
# The new proxy mode keeps API keys secure on the server-side

# Application Configuration  
NEXT_PUBLIC_APP_URL=http://localhost:3001
