# GrowthKit Configuration (Public Key Mode)
# Get your public key from the GrowthKit admin dashboard → API Tokens tab
# Public keys are safe to use in client-side code
NEXT_PUBLIC_GROWTHKIT_PUBLIC_KEY=pk_your_public_key_here

# GrowthKit API URL (Optional - defaults to https://growth.fenixblack.ai/api)
# Only set this if you're using a custom deployment
# NEXT_PUBLIC_GROWTHKIT_API_URL=https://your-custom-domain.com/api

# Application Configuration  
NEXT_PUBLIC_APP_URL=http://localhost:3001

# Note: No server-side API keys needed with public key mode!
# Public keys generate secure, time-limited tokens automatically.
